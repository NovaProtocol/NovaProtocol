[project]
name = "novaprotocol"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py313"
lint.select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
lint.ignore = []

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint.per-file-ignores]
"apps/console_svg.py" = ["E501"]
"apps/skills_svg.py" = ["E501"]
"apps/name_svg.py" = ["E501"]
"utilities/terminal_svg/render.py" = ["E501"]
"documentation/app.py" = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["apps", "utilities", "data"]

[tool.mypy]
python_version = "3.14"
strict = false
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
exclude = "documentation"

[tool.yamllint]
extends = "default"
rules.line-length.max = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
